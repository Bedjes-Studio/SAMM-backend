const Cost = require('../models/cost');

exports.getAll = (req, res, next) => {
    //TODO
    // Cost.find().then(
    //     (cost) => {
    //         res.status(200).json(cost);
    //     }
    // ).catch(
    //     (error) => {
    //         res.status(400).json({
    //             error: error
    //         });
    //     }
    // );
};